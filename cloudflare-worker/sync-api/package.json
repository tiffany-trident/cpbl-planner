{
  "name": "cpbl-planner-api",
  "version": "0.0.1",
  "private": true,
  "description": "CPBL Planner sync API — Google OAuth + KV state store",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.80.0"
  }
}
